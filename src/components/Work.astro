---
const cases = [
  {
    num: '01',
    tag: 'B · Editorial',
    title: 'Aesthetic Clinic',
    description: 'Identity and launch site for a London clinic. Soft, editorial, built to earn trust.',
    status: 'In production',
    image: 'https://images.unsplash.com/photo-1616394584738-fc6e612e71b9?auto=format&fit=crop&w=1600&q=80',
    href: '#contact',
  },
  {
    num: '02',
    tag: 'A · Cinematic',
    title: 'Kinetic Film Studio',
    description: 'A fully interactive site for a film production company. WebGL, scroll-driven, bold.',
    status: 'Showcase',
    image: 'https://images.unsplash.com/photo-1478720568477-152d9b164e26?auto=format&fit=crop&w=1200&q=80',
    href: '#contact',
  },
  {
    num: '03',
    tag: 'C · Brand-forward',
    title: 'Live Life Customs',
    description: 'London streetwear studio. Custom apparel built to order, Instagram-only intake.',
    status: 'Live',
    image: '/images/work/llc/logo.png',
    imageFit: 'contain',
    href: '/work/livelifecustoms',
  },
];
---

<section id="work" class="section work">
  <div class="frame">
    <header class="work-head">
      <div class="work-head-left">
        <span class="eyebrow">01 &middot; Selected Work</span>
        <h2 class="display-2 reveal-words" data-reveal-headline>
          The work speaks <em class="italic-accent">for itself.</em>
        </h2>
        <p class="lead fade-up work-head-lead">
          Small roster. Every project built properly.
        </p>
      </div>
    </header>

    <div class="work-selector" data-work-selector>
      {cases.map((c, i) => (
        <a
          href={c.href}
          class={`work-panel ${i === 0 ? 'is-active' : ''} ${c.imageFit === 'contain' ? 'is-logo' : ''} ${c.isOpenSlot ? 'is-open' : ''}`}
          data-index={i}
          aria-label={`${c.title} — ${c.tag}`}
          style={c.image && c.imageFit !== 'contain' ? `--panel-bg: url('${c.image}'); background-image: var(--panel-bg);` : ''}
        >
          {c.imageFit === 'contain' && c.image && (
            <img class="work-panel-logo" src={c.image} alt="" loading="lazy" decoding="async" />
          )}
          {c.isOpenSlot && (
            <div class="work-panel-plus font-display" aria-hidden="true">+</div>
          )}
          <div class="work-panel-overlay" aria-hidden="true"></div>
          <div class="work-panel-num font-mono">{c.num}</div>
          <div class="work-panel-status font-mono">{c.status}</div>
          <div class="work-panel-label">
            <div class="work-panel-tag font-mono">{c.tag}</div>
            <h3 class="work-panel-title">{c.title}</h3>
            <p class="work-panel-desc">{c.description}</p>
          </div>
        </a>
      ))}
    </div>
  </div>
</section>

<style>
  .work {
    padding-top: clamp(72px, 8vw, 120px);
    padding-bottom: clamp(64px, 7vw, 96px);
  }

  .work-head {
    display: grid;
    grid-template-columns: 1fr;
    gap: clamp(20px, 2vw, 32px);
    margin-bottom: clamp(32px, 4vw, 56px);
    max-width: 720px;
  }
  .work-head-left { display: grid; gap: 20px; }
  .work-head-lead { margin-top: 4px; }

  /* Horizontal accordion */
  .work-selector {
    display: flex;
    gap: 6px;
    height: clamp(420px, 50vh, 540px);
    isolation: isolate;
  }

  .work-panel {
    flex: 1 1 0%;
    position: relative;
    overflow: hidden;
    cursor: pointer;
    border: 1px solid var(--color-border);
    border-radius: 6px;
    background-color: #18181b;
    background-size: cover;
    background-position: center;
    box-shadow: 0 12px 30px -22px rgba(20, 20, 24, 0.4);
    min-width: 60px;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    transition:
      flex 0.7s var(--ease-out-expo),
      border-color 0.5s var(--ease-out-expo),
      box-shadow 0.5s var(--ease-out-expo);
    will-change: flex-grow;
  }
  .work-panel.is-active {
    flex: 5 1 0%;
    border-color: var(--color-border-active);
    box-shadow: 0 24px 60px -28px rgba(20, 20, 24, 0.5);
  }
  .work-panel:focus-visible {
    outline: 2px solid var(--color-lime);
    outline-offset: 2px;
  }

  /* LLC logo panel — light surface, contained logo */
  .work-panel.is-logo {
    background-color: #FAFAFA;
  }
  .work-panel-logo {
    position: absolute;
    top: 50%;
    left: 50%;
    width: clamp(64px, 38%, 200px);
    aspect-ratio: 1 / 1;
    object-fit: contain;
    transform: translate(-50%, -50%);
    pointer-events: none;
  }

  /* Open-slot panel — dashed, cream */
  .work-panel.is-open {
    background-color: var(--color-surface);
    border-style: dashed;
    border-color: var(--color-border-active);
  }
  .work-panel-plus {
    position: absolute;
    inset: 0;
    display: grid;
    place-items: center;
    font-size: clamp(48px, 5vw, 96px);
    line-height: 1;
    color: var(--color-ink-faint);
    pointer-events: none;
    transition: color 0.5s var(--ease-out-expo), transform 0.6s var(--ease-out-expo);
  }
  .work-panel.is-open:hover .work-panel-plus,
  .work-panel.is-open.is-active .work-panel-plus {
    color: var(--color-cream);
    transform: rotate(90deg);
  }

  /* Overlay — darker gradient on dark panels, light on logo/open panels */
  .work-panel-overlay {
    position: absolute;
    inset: 0;
    pointer-events: none;
    background: linear-gradient(180deg, rgba(20, 20, 24, 0) 0%, rgba(20, 20, 24, 0) 45%, rgba(20, 20, 24, 0.78) 100%);
    opacity: 0;
    transition: opacity 0.7s var(--ease-out-expo);
  }
  .work-panel.is-active .work-panel-overlay { opacity: 1; }
  .work-panel.is-logo .work-panel-overlay {
    background: linear-gradient(180deg, rgba(250, 250, 250, 0) 0%, rgba(250, 250, 250, 0) 45%, rgba(250, 250, 250, 0.92) 100%);
  }
  .work-panel.is-open .work-panel-overlay { background: none; }

  /* Top corner labels */
  .work-panel-num {
    position: absolute;
    top: 14px;
    left: 16px;
    font-size: 11px;
    letter-spacing: 0.18em;
    color: rgba(245, 240, 229, 0.92);
    text-shadow: 0 1px 6px rgba(0, 0, 0, 0.45);
    z-index: 2;
  }
  .work-panel-status {
    position: absolute;
    top: 14px;
    right: 16px;
    font-size: 10px;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: rgba(245, 240, 229, 0.92);
    padding: 5px 10px;
    border: 1px solid rgba(245, 240, 229, 0.4);
    border-radius: 999px;
    background: rgba(20, 20, 24, 0.4);
    backdrop-filter: blur(6px);
    -webkit-backdrop-filter: blur(6px);
    z-index: 2;
  }
  .work-panel.is-logo .work-panel-num,
  .work-panel.is-open .work-panel-num {
    color: var(--color-ink-dim);
    text-shadow: none;
  }
  .work-panel.is-logo .work-panel-status,
  .work-panel.is-open .work-panel-status {
    color: var(--color-ink);
    border-color: var(--color-border-active);
    background: rgba(255, 255, 255, 0.7);
    backdrop-filter: none;
    -webkit-backdrop-filter: none;
  }

  /* Bottom label — fades in only when active */
  .work-panel-label {
    position: relative;
    z-index: 2;
    padding: 24px 26px 26px;
    color: #fff;
    pointer-events: none;
  }
  .work-panel-tag,
  .work-panel-title,
  .work-panel-desc {
    opacity: 0;
    transform: translateY(10px);
    transition: opacity 0.5s var(--ease-out-expo), transform 0.5s var(--ease-out-expo);
  }
  .work-panel.is-active .work-panel-tag {
    opacity: 1;
    transform: translateY(0);
    transition-delay: 0.18s;
  }
  .work-panel.is-active .work-panel-title {
    opacity: 1;
    transform: translateY(0);
    transition-delay: 0.24s;
  }
  .work-panel.is-active .work-panel-desc {
    opacity: 1;
    transform: translateY(0);
    transition-delay: 0.3s;
  }
  .work-panel-tag {
    font-size: 11px;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--color-cream);
    margin-bottom: 10px;
  }
  .work-panel-title {
    font-family: var(--font-display);
    font-weight: 400;
    font-size: clamp(26px, 2.6vw, 42px);
    line-height: 1.05;
    letter-spacing: -0.015em;
    color: #fff;
    text-shadow: 0 1px 12px rgba(0, 0, 0, 0.45);
    margin-bottom: 6px;
    padding-bottom: 0.04em;
  }
  .work-panel-desc {
    font-family: var(--font-sans);
    font-size: 14px;
    line-height: 1.5;
    color: rgba(245, 240, 229, 0.88);
    text-shadow: 0 1px 8px rgba(0, 0, 0, 0.5);
    max-width: 48ch;
  }
  .work-panel.is-logo .work-panel-title { color: var(--color-ink); text-shadow: none; }
  .work-panel.is-logo .work-panel-desc { color: var(--color-ink-muted); text-shadow: none; }
  .work-panel.is-open .work-panel-title { color: var(--color-ink); text-shadow: none; }
  .work-panel.is-open .work-panel-desc { color: var(--color-ink-muted); text-shadow: none; }

  @media (max-width: 900px) {
    .work-head { grid-template-columns: 1fr; gap: 28px; }
    .work-selector {
      flex-direction: column;
      height: auto;
      gap: 16px;
      isolation: auto;
    }
    /* On mobile: each panel becomes a stacked card with image up top + always-visible text below.
       The desktop expanding-panel behaviour (only active shows text) is dropped — it relies on hover
       and tap-twice-to-navigate, both awkward on touch. */
    .work-panel,
    .work-panel.is-active {
      flex: none;
      height: auto;
      display: grid;
      grid-template-rows: 220px auto;
      background-image: none !important;
      background-color: var(--color-card);
      border-color: var(--color-border-active);
      border-radius: 10px;
      overflow: hidden;
      box-shadow: 0 18px 50px -34px rgba(20, 20, 24, 0.22);
    }
    .work-panel::before {
      content: "";
      grid-row: 1;
      background-image: var(--panel-bg, none);
      background-size: cover;
      background-position: center;
      background-color: #18181b;
      position: relative;
    }
    .work-panel.is-logo::before {
      background-color: #FAFAFA;
      background-image: none;
    }
    .work-panel.is-open::before {
      background-color: var(--color-surface);
      background-image: none;
    }
    /* Move corner labels onto the image area only */
    .work-panel-num,
    .work-panel-status {
      top: 14px;
    }
    /* Text panel below image — always visible on mobile, no active-only logic */
    .work-panel-overlay { display: none; }
    .work-panel-label {
      grid-row: 2;
      padding: 20px 22px 22px;
      background: var(--color-card);
      color: var(--color-ink);
    }
    .work-panel-tag,
    .work-panel-title,
    .work-panel-desc {
      opacity: 1;
      transform: none;
      transition: none;
    }
    .work-panel-tag { color: var(--color-cream); }
    .work-panel-title {
      color: var(--color-ink);
      text-shadow: none;
      font-size: clamp(24px, 6vw, 32px);
    }
    .work-panel-desc {
      color: var(--color-ink-muted);
      text-shadow: none;
      font-size: 14px;
    }
    /* Logo panel: keep contained logo centred on the image area */
    .work-panel.is-logo .work-panel-logo {
      top: 110px;
      width: clamp(80px, 28vw, 140px);
    }
    /* Open-slot mobile (rare) */
    .work-panel.is-open .work-panel-plus {
      grid-row: 1;
      inset: auto;
      height: 220px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .work-panel,
    .work-panel-tag,
    .work-panel-title,
    .work-panel-desc,
    .work-panel-overlay {
      transition: none !important;
    }
    .work-panel { opacity: 1; transform: none; }
  }
</style>

<script>
  const selector = document.querySelector<HTMLElement>('[data-work-selector]');
  if (selector) {
    const panels = Array.from(selector.querySelectorAll<HTMLElement>('.work-panel'));

    // Hover/focus = activate panel; touch first-tap also activates, second tap navigates
    panels.forEach((panel) => {
      const activate = () => {
        if (panel.classList.contains('is-active')) return;
        panels.forEach((p) => p.classList.remove('is-active'));
        panel.classList.add('is-active');
      };
      panel.addEventListener('mouseenter', activate);
      panel.addEventListener('focus', activate);
      panel.addEventListener('click', (e) => {
        if (matchMedia('(hover: none)').matches && !panel.classList.contains('is-active')) {
          e.preventDefault();
          activate();
        }
      });
    });
  }
</script>
