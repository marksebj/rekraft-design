---
---
<section class="hero">
  <div class="hero-bg" aria-hidden="true">
    <div class="hero-grain"></div>
    <div class="hero-glow"></div>
  </div>

  <div class="frame hero-inner">
    <div class="hero-top">
      <span class="eyebrow" data-hero-eyebrow>
        Independent design studio · London
      </span>
      <span class="hero-year font-mono" data-hero-eyebrow>Est. 2026</span>
    </div>

    <div class="hero-body">
      <h1 class="display-1 reveal-words" data-hero-headline>
        Made to stand out.<br /><em class="italic-accent">Built to stand up.</em>
      </h1>

      <aside class="hero-tile" aria-hidden="true">
        <div class="hero-tile-img">
          <img
            src="https://images.unsplash.com/photo-1618004912476-29818d81ae2e?auto=format&fit=crop&w=900&q=80"
            alt=""
            loading="eager"
            fetchpriority="high"
          />
        </div>
        <div class="hero-tile-meta font-mono">
          <span>Now building</span>
          <span>Aesthetic Clinic · London</span>
        </div>
      </aside>
    </div>

    <div class="hero-foot">
      <p class="lead" data-hero-sub>
        Brand, websites, and social for brands that mean business.
      </p>
      <div class="hero-ctas" data-hero-ctas>
        <a href="#work" class="btn btn-primary">
          See the work <span class="btn-arrow">&rarr;</span>
        </a>
        <a href="#contact" class="btn btn-ghost">Start a project</a>
      </div>
    </div>

    <div class="hero-scroll-cue" aria-hidden="true">
      <span>scroll</span>
      <span class="hero-scroll-line"></span>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100svh;
    display: grid;
    align-items: end;
    padding-top: 120px;
    padding-bottom: clamp(48px, 6vw, 80px);
    position: relative;
    overflow: hidden;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    z-index: 0;
    pointer-events: none;
  }
  .hero-glow {
    position: absolute;
    top: -20%;
    right: -10%;
    width: 75%;
    height: 90%;
    background: radial-gradient(ellipse at 70% 40%, var(--color-ambient) 0%, transparent 55%);
  }
  .hero-grain {
    position: absolute;
    inset: 0;
    background-image:
      radial-gradient(circle at 1px 1px, rgba(20, 20, 24, 0.06) 1px, transparent 0);
    background-size: 3px 3px;
    opacity: 0.55;
    mix-blend-mode: multiply;
  }

  .hero-inner {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-rows: auto 1fr auto;
    gap: clamp(32px, 4vw, 64px);
    min-height: calc(100svh - 120px);
    width: 100%;
  }

  .hero-top {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 20px;
  }
  .hero-year {
    font-size: 11px;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: var(--color-ink-dim);
  }

  .hero-body {
    display: grid;
    grid-template-columns: 1fr auto;
    align-items: end;
    gap: clamp(32px, 4vw, 80px);
    width: 100%;
  }

  .hero h1 {
    max-width: 18ch;
    margin-top: clamp(20px, 4vw, 60px);
  }
  .hero h1 em { font-style: italic; }

  .hero-tile {
    width: clamp(220px, 22vw, 340px);
    display: grid;
    gap: 12px;
    align-self: end;
    margin-bottom: 4px;
  }
  .hero-tile-img {
    aspect-ratio: 4 / 5;
    border-radius: 10px;
    overflow: hidden;
    border: 1px solid var(--color-border);
    background: var(--color-surface);
    box-shadow: 0 12px 40px -20px rgba(20, 20, 24, 0.35);
  }
  .hero-tile-img img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    filter: contrast(1.04) saturate(1.05);
    transition: transform 8s var(--ease-out-expo);
  }
  .hero-tile:hover .hero-tile-img img { transform: scale(1.04); }
  .hero-tile-meta {
    display: flex;
    justify-content: space-between;
    gap: 12px;
    font-size: 10px;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: var(--color-ink-dim);
  }
  .hero-tile-meta span:first-child { color: var(--color-cream); }

  .hero-foot {
    display: grid;
    grid-template-columns: 1fr auto;
    align-items: end;
    gap: 48px;
    padding-top: 24px;
    border-top: 1px solid var(--color-border);
  }
  .hero-ctas {
    display: flex;
    gap: 12px;
    flex-wrap: wrap;
  }

  .hero-scroll-cue {
    position: absolute;
    right: 0;
    bottom: 20px;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 14px;
    font-family: var(--font-mono);
    font-size: 10px;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--color-ink-dim);
    writing-mode: vertical-rl;
  }
  .hero-scroll-line {
    writing-mode: horizontal-tb;
    display: block;
    width: 1px;
    height: 56px;
    background: linear-gradient(to bottom, var(--color-ink-dim), transparent);
    animation: scroll-pulse 2.4s var(--ease-out-expo) infinite;
  }
  @keyframes scroll-pulse {
    0%, 100% { transform: scaleY(0.5); transform-origin: top; opacity: 0.4; }
    50% { transform: scaleY(1); transform-origin: top; opacity: 1; }
  }

  @media (max-width: 1100px) {
    .hero-body { grid-template-columns: 1fr; gap: 24px; }
    .hero-tile { justify-self: start; width: min(300px, 70%); }
  }
  @media (max-width: 900px) {
    .hero { padding-top: 100px; }
    .hero-foot {
      grid-template-columns: 1fr;
      gap: 24px;
    }
    .hero-scroll-cue { display: none; }
  }
  @media (max-width: 520px) {
    .hero-top { flex-direction: column; align-items: flex-start; gap: 8px; }
    .hero-tile { display: none; }
  }
</style>
